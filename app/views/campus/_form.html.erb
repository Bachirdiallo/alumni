<%= form_for(@campu) do |f| %>
  <% if @campu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campu.errors.count, "error") %> prohibited this campu from being saved:</h2>

      <ul>
      <% @campu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<br>
  <div class="panel panel-default">
    <div class="panel-heading"><strong>New Campus</strong></div>
    <div class="panel-body">
      <div class="col-md-8" >
        <%= f.text_field :name, placeholder: 'Name', autofocus: true, autocomplete: "off", :class => 'form-control empty' %>
      </div>

      <div class="col-md-4">
        <%= f.submit "Save", :class => 'btn-u'%>
        <%= link_to 'Back', campus_path, :class => 'btn-u btn-u-default' %>
      </div>
    </div>
  </div>
<% end %>
