<!DOCTYPE html>
<html>
  <head>
    <title>Alumni</title>
    <%= stylesheet_link_tag    'application'%>
    <%= csrf_meta_tags %>
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.0/jquery.min.js"></script>
    <script src="http://maps.google.com/maps/api/js?sensor=false"></script>
  </head>
    <body>
      <div id="bar">
        <div class="row backgrnd">
          <div class="col-md-12 ">
            <div class="col-md-2">

            </div>
            <div class="col-md-10">
              <%= render 'find_users/locate_user'%>
              Your locaiton has changed, would you like to see the alumni in your current city?
              <%= link_to "Find", find_users_path, :class => 'btn btn-primary btn-sm' %>
              <button type="button" name="button" class="btn btn-primary btn-sm .no-padding" id= "button">Hide</button>
            </div>
          </div>
        </div>
      </div>
        <div class="container no-padding">
          <div class="row">
            <div class="col-md-12 backgrnd">

                <%= render 'layouts/navigation' %>

              <% if user_signed_in?%>
                <% if !current_user.is_graduate %>
                    <%= render 'layouts/sidebar' %>
                <%end%>
              <%end%>
              <%= yield %>
            </div>
        <%= javascript_include_tag 'application'%>
    </body>
    <style >
      .backgrnd{padding: 0px; background-color: white}
      .pad{padding: 10px;}
      .no-padding{padding: 0px}
      .pad-top{padding-top: 5px}
    </style>

    <script>
      $("#bar").hide();
    "  <% if user_signed_in?%>"
      var old_city = "<%= current_user.city%>";
      var current_city = window.name;

      if(current_city != old_city){
        $("#bar").show();
      }
      else{
        $("#bar").hide();
      }
      $("#button").click(function(){
        $("#bar").hide();
      });
      "<%end%>"
    </script>
</html>
