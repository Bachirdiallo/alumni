    <!-- Navigation -->
    <nav class="navbar navbar-default" role="navigation">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#menu-items">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>

            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse pull-right" id="menu-items">
                <ul class="nav navbar-nav">
                    <li>
                        <%= link_to user_profiles_path do %> <span class="fa fa-user"> </span> <% end %>
                    </li>
                    <li>
                        <%= link_to find_users_path do %> <span class="fa fa-search"></span> <% end %>
                    </li>
                    <li>
                      <% if current_user %>
                        <%= link_to privacy_user_profiles_path(id: current_user.id) do %> <span class="fa fa-cog fa-fw"></span><% end %>
                      <% end %>
                    </li>
                    <li>
                      <% if current_user %>
                        <%= link_to destroy_user_session_path, :class => 'pull-left', :method => :delete do %> <span class="glyphicon glyphicon-log-out"></span> <% end %>
                        <% end %>
                    </li>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>
