<div class="col-sm-9">
  <%= form_for(@announcement) do |f| %>
    <% if @announcement.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@announcement.errors.count, "error") %> prohibited this announcement from being saved:</h2>

        <ul>
        <% @announcement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>


    <div >
      <%= f.text_field :title, :class => 'form-control' %>
    </div>

    <div >
      <%= f.bootsy_area(:content, :placeholder => "Content", editor_options: { image: true }, :class => "form-control").html_safe %>

    </div>
      <%= f.submit "Post Announcement", :class => 'btn btn-primary btn-md' %>
      <%= link_to 'Back', announcements_path, :class => 'btn btn-primary btn-md' %>
  <% end %>
</div>
